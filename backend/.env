# PLACEHOLDER - this file is overwritten automatically by
# .github/workflows/deploy.yml from the PRODUCTION_ENV repo secret
# on every push to main (or manual workflow_dispatch run).
APP_ENV=production
APP_DEBUG=false
